import 'package:flutter/material.dart';

class BeutiPage extends StatelessWidget {
  const BeutiPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child:Text(
            'BeutiPage'
        ),
      ),
    );
  }
}
